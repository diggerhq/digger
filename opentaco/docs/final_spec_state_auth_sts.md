# OpenTaco — State + Auth + STS (Spec Stub)

This document will host the finalized spec for OIDC login, OpenTaco JWTs (access/refresh + JWKS), STS issuance for S3-compatible endpoint, and RBAC across API and S3 paths.

Refer to `agents_todo/auth-oidc-rbac.md` for the authoritative spec until this is populated.

