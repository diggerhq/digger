# Backend Profile Guide (Stub)

This guide explains configuring the global AWS profile `opentaco-state-backend` to use `taco creds --json` via `credential_process`, and pointing Terraform's `backend "s3"` at the OpenTaco S3-compatible endpoint.

Content to be completed when auth/STS is implemented.

