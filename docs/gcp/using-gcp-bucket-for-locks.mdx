---
title: "Using GCP bucket for locks"
---

You can configure Digger to use a GCP bucket for locks. To do that, set the following 2 settings with your bucket name:

- `GOOGLE_STORAGE_LOCK_BUCKET` env variable
- `google-lock-bucket` action input
