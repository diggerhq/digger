apiVersion: v1
kind: Service
metadata:
  name: {{ include "taco-ui.fullname" . }}
  labels:
    {{- include "taco-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taco-ui.selectorLabels" . | nindent 4 }}

