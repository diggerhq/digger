# RBAC Test Data Fixtures

This directory contains test data fixtures for RBAC integration tests.

## Structure

- `permissions/` - Permission definitions
- `roles/` - Role definitions  
- `users/` - User assignment definitions
- `units/` - Sample unit (terraform state) data

## Usage

These fixtures are used by the RBAC integration tests to simulate realistic RBAC scenarios.

